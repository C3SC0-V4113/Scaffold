---
'purrfold': minor
---

Initialize every generated app as a consistent, commit-free Git repository on `main`, and upgrade React Doctor to 0.9.4 so Astro React files receive full project analysis without blanket dependency suppression.
