# OpenSpec configuration for scaffold-next-quality (purrfold)
# Auto-generated by sdd-init on 2026-07-07

project: scaffold-next-quality
package: purrfold
stack:
  runtime: Node.js >=20.9
  language: TypeScript (ESM)
  bundler: tsup (single dist/index.js)
  release: Changesets
  ci: GitHub Actions (npm run check)

context: >
  Standalone Node/TypeScript ESM CLI package that scaffolds new Next.js apps
  with shadcn/ui, strict quality tooling (ESLint, Prettier, react-doctor),
  agent docs, Claude hooks, and optional tests (Vitest + Playwright).
  Supports npm, pnpm, and bun. Architecture uses Commander CLI framework,
  Executor abstraction for testability, and template-based file generation.

strict_tdd: true
  # Derived: no strict_tdd marker/config found, but vitest test runner exists.
  # Per SDD convention, available test runner → strict_tdd: true.

testing:
  runner:
    framework: vitest
    command: npm run test
    coverage: false
    # vitest supports --coverage but no coverage command is configured
  layers:
    unit: vitest
    integration: vitest + DryRunExecutor pattern
    e2e: scripts/cli-e2e.mjs + vitest harness validation
  quality:
    type_checker: tsc --noEmit
    linter: none (project itself has no eslintrc; generates eslint for scaffolded apps)
    formatter: none (project itself has no prettier config)
  check_gate: npm run check (typecheck + test + build)

phases:
  proposal: sdd-propose
  spec: sdd-spec
  design: sdd-design
  tasks: sdd-tasks
  apply: sdd-apply
  verify: sdd-verify
  archive: sdd-archive
